# Node / Quartz
node_modules
public
.quartz-cache
tsconfig.tsbuildinfo
prof

# OS / Editor
.DS_Store
Thumbs.db

# Obsidian workspace state (keep the rest of .obsidian if you want plugin sync)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Secrets
.env
.env.*

# Local-only docs (contain exposed/rotated API keys — do not publish)
SETUP_COMPLETE.md
.claude/
